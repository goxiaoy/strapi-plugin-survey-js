{
  "routes": [
    {
      "method": "GET",
      "path": "/settings",
      "handler": "survey-js.getSettings",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/settings",
      "handler": "survey-js.updateSettings",
      "config": {
        "policies": []
      }
    }
  ]
}
